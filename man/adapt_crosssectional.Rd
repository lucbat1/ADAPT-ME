% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapt_crosssectional.R
\name{adapt_crosssectional}
\alias{adapt_crosssectional}
\title{ADAPT-ME Cross-Sectional Ablation}
\usage{
adapt_crosssectional(
  input_data,
  cond.var,
  base.cond = NULL,
  adj.var = NULL,
  censor = 1,
  prev.filter = 0.05,
  depth.filter = 1000,
  alpha = 0.05
)
}
\arguments{
\item{input_data}{a phyloseq object}

\item{cond.var}{condition variable name (character)}

\item{base.cond}{baseline level for categorical conditions}

\item{adj.var}{adjustment variable names (character vector or NULL)}

\item{censor}{censoring value for zero counts (default 1)}

\item{prev.filter}{prevalence filter threshold (default 0.05)}

\item{depth.filter}{minimum library size (default 1000)}

\item{alpha}{BH-adjusted p-value cutoff (default 0.05)}
}
\value{
a \code{DAresult} object (same class as \code{adapt()}); use \code{summary()}
and \code{plot()} normally
}
\description{
Runs the full ADAPT-ME pipeline (BUM-guided reference selection +
Tobit count-ratio tests) but with \strong{no subject random intercept}.
Every observation is treated as an independent draw, identical to the
behaviour of the original cross-sectional ADAPT framework.

This function exists solely as an ablation baseline. It is intentionally
isolated: it shares no internal code paths with \code{adapt()} and does not
require a \code{subject.var}.

The reference score uses only median-distance (original ADAPT criterion),
not the stability term added by ADAPT-ME, so the comparison isolates the
single methodological difference: \strong{random effects vs. no random effects}.
}
